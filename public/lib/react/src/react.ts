import * as React from "react";
import * as ReactDOMClient from "react-dom/client";
import * as ReactDOMLegacy from "react-dom"

const ReactDOM = ReactDOMClient;

export { React, ReactDOM, ReactDOMLegacy };
